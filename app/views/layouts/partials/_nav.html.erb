<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">

            <!-- Collapsed Hamburger -->
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#app-navbar-collapse" aria-expanded="false">
                <span class="sr-only">Toggle Navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <!-- Branding Image -->
            <%= link_to !!current_area ? current_area.name : 'Home', root_path, class: 'navbar-brand' %>
        </div>

        <div class="collapse navbar-collapse" id="app-navbar-collapse">
            <!-- Left Side Of Navbar -->
            <ul class="nav navbar-nav">
                <li>
                    <%= link_to 'Categories', categories_area_path(current_area) if !!current_area %>
                </li>
            </ul>

            <!-- Right Side Of Navbar -->
            <ul class="nav navbar-nav navbar-right">
                <!-- Authentication Links -->
                <!-- logged in links -->
                <% if user_signed_in? %>
                    <li>
                        <!-- <a href="{{ route('listings.favorites.index', [$area]) }}">Favorites</a> -->
                    </li>
                    <li>
                        <!-- <a href="{{ route('listings.viewed.index', [$area]) }}">Recently Viewed</a> -->
                    </li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true" v-pre>
                            Organizations <span class="caret"></span>
                        </a>

                        <ul class="dropdown-menu">
                            <li>
                                <%= link_to "New Organization", new_area_organization_path(current_area || 'united-states') %>
                            </li>

                            <li>
                               <%= link_to "Your Owned Orgs", profile_own_organizations_path %>
                            </li>

                            <li>
                                <%= link_to "Pending Claims", profile_claimed_organizations_path %>
                            </li>

                            <li>
                                <%= link_to "Favorite Organizations", profile_favorite_organizations_path %>
                            </li>                            
                        </ul>
                    </li>

                    <app-notifications></app-notifications>

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true" v-pre>
                            <%= current_user.email %> <span class="caret"></span>
                        </a>

                        <ul class="dropdown-menu">
                            <li>
                                <%= link_to "Log out", destroy_user_session_path, method: :delete %>
                            </li>
                        </ul>
                    </li>
                <% else %>
                <!-- guest links @guest -->
                    <li>
                        <%= link_to "Log In", new_user_session_path %>
                    </li>
                    <li>
                        <%= link_to "Register", new_user_registration_path %>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>